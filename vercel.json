{
  "version": 2,
  "buildCommand": "pnpm nx build docs --configuration=production",
  "outputDirectory": "dist/apps/docs",
  "framework": "angular",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}